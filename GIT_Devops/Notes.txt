git remote add origin https://github.com/jyothisree2000/Devops.git
#username and email config
git config --global user.name "Your Name"
git config --global user.email "youremail@yourdomain.com"
git push --set-upstream origin gitB
# GIT MERGE####
go to master (original branch) by git checkout master then do git merge gitB
git commit -m "merge git push
git mergetool
used the same file to commit at once, then got conflict. did git pull then enter "git mergetool" (no filename). resolve the conflict , update the stream
### Delete a branch###
git branch -d gitB to delete a local branch
to delete a remote branch >> git branch <remote> -d gitB (this didnt work)
### git rebase ###
$ git checkout test
git config pull.rebase false >>>helped with conflicts
git pull origin master --rebas
$ git rebase master
$ git log --graph --pretty=oneline
